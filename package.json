{
  "name": "ai-math-tutor",
  "version": "1.0.0",
  "private": true,
  "description": "AI Math Tutor Application",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=index.js --jsx=automatic --format=esm"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "@google/genai": "^1.28.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}
